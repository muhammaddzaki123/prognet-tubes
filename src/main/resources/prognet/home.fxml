<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Button?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<StackPane xmlns="http://javafx.com/javafx/8.0.171" xmlns:fx="http://javafx.com/fxml/1"
    fx:controller="prognet.controller.HomeController" styleClass="root-container" stylesheets="@styles.css">
    <children>
        <!-- Include Decorative Animals -->
        <fx:include source="base.fxml" />

        <!-- Main Content -->
        <VBox alignment="CENTER" spacing="20.0" styleClass="content-card" maxWidth="600" maxHeight="550">
            <children>
                <!-- Icon -->
                <Label text="🎮"
                    style="-fx-font-size: 48; -fx-background-color: white; -fx-padding: 20; -fx-background-radius: 15; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 2);">
                    <VBox.margin>
                        <Insets bottom="10" />
                    </VBox.margin>
                </Label>

                <!-- Title -->
                <Label text="Memory Game" styleClass="title-purple">
                    <VBox.margin>
                        <Insets bottom="5" />
                    </VBox.margin>
                </Label>

                <!-- Subtitle -->
                <Label text="Match the animals and have fun!" styleClass="label-secondary">
                    <VBox.margin>
                        <Insets bottom="30" />
                    </VBox.margin>
                </Label>

                <!-- Create Room Button -->
                <Button fx:id="createRoomBtn" text="👥  Create Room" onAction="#onCreateRoom" styleClass="btn-green"
                    prefWidth="280" maxWidth="480">
                    <VBox.margin>
                        <Insets bottom="10" />
                    </VBox.margin>
                </Button>

                <!-- Join Room Button -->
                <Button fx:id="joinRoomBtn" text="🚺  Join Room" onAction="#onJoinRoom" styleClass="btn-blue"
                    prefWidth="280" maxWidth="480">
                    <VBox.margin>
                        <Insets bottom="10" />
                    </VBox.margin>
                </Button>

                <!-- How to Play Button -->
                <Button fx:id="howToPlayBtn" text="❓  How to Play" onAction="#onHowToPlay"
                    styleClass="btn-purple-outline" prefWidth="280" maxWidth="480">
                    <VBox.margin>
                        <Insets bottom="10" />
                    </VBox.margin>
                </Button>

                <!-- Configure Network Button -->
                <Button fx:id="configNetworkBtn" text="⚙  Configure Network" onAction="#onConfigureNetwork"
                    styleClass="btn-purple-outline" prefWidth="280" maxWidth="480">
                    <VBox.margin>
                        <Insets bottom="20" />
                    </VBox.margin>
                </Button>

                <!-- Footer Text -->
                <Label text="Play with friends on the same WiFi!" style="-fx-font-size: 12; -fx-text-fill: #999999;">
                    <VBox.margin>
                        <Insets top="10" />
                    </VBox.margin>
                </Label>
            </children>
        </VBox>
    </children>
</StackPane>