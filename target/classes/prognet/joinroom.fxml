<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.TextField?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Font?>

<StackPane fx:id="rootContainer" xmlns="http://javafx.com/javafx/8.0.171" xmlns:fx="http://javafx.com/fxml/1"
    fx:controller="prognet.controller.JoinRoomController" styleClass="root-container" stylesheets="@styles.css">
    <children>
        <!-- Include Decorative Animals -->
        <fx:include source="base.fxml" />

        <!-- Main Content Card -->
        <VBox alignment="TOP_CENTER" spacing="20.0" styleClass="content-card" maxWidth="600" maxHeight="650">
            <children>
                <!-- Header with Back Button -->
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <children>
                        <!-- Back Button -->
                        <Button fx:id="backBtn" text="←  Back" onAction="#onBack" styleClass="btn-back">
                        </Button>
                    </children>
                    <VBox.margin>
                        <Insets bottom="10" />
                    </VBox.margin>
                </HBox>

                <!-- Title -->
                <Label text="Join Room" styleClass="title-blue">
                    <VBox.margin>
                        <Insets bottom="5" />
                    </VBox.margin>
                </Label>

                <!-- Subtitle -->
                <Label text="Enter Room Code" styleClass="label-secondary">
                    <VBox.margin>
                        <Insets bottom="20" />
                    </VBox.margin>
                </Label>

                <!-- Room Code Input Section -->
                <VBox alignment="CENTER" spacing="15">
                    <children>
                        <!-- Room Code Digit Inputs -->
                        <HBox alignment="CENTER" spacing="10">
                            <children>
                                <TextField fx:id="digit1" prefWidth="60" prefHeight="70" styleClass="room-code-input"
                                    alignment="CENTER" promptText="•" maxWidth="60">
                                </TextField>
                                <TextField fx:id="digit2" prefWidth="60" prefHeight="70" styleClass="room-code-input"
                                    alignment="CENTER" promptText="•" maxWidth="60">
                                </TextField>
                                <TextField fx:id="digit3" prefWidth="60" prefHeight="70" styleClass="room-code-input"
                                    alignment="CENTER" promptText="•" maxWidth="60">
                                </TextField>
                                <TextField fx:id="digit4" prefWidth="60" prefHeight="70" styleClass="room-code-input"
                                    alignment="CENTER" promptText="•" maxWidth="60">
                                </TextField>
                                <TextField fx:id="digit5" prefWidth="60" prefHeight="70" styleClass="room-code-input"
                                    alignment="CENTER" promptText="•" maxWidth="60">
                                </TextField>
                                <TextField fx:id="digit6" prefWidth="60" prefHeight="70" styleClass="room-code-input"
                                    alignment="CENTER" promptText="•" maxWidth="60">
                                </TextField>
                            </children>
                        </HBox>
                    </children>
                    <VBox.margin>
                        <Insets bottom="10" />
                    </VBox.margin>
                </VBox>

                <!-- Full Code Input (Alternative) -->
                <TextField fx:id="fullCodeInput" promptText="Enter 6-digit code" styleClass="full-code-input"
                    prefWidth="340" maxWidth="340" alignment="CENTER">
                    <VBox.margin>
                        <Insets bottom="20" />
                    </VBox.margin>
                </TextField>

                <!-- Join Button -->
                <Button fx:id="joinBtn" text="🚪  Join Room" onAction="#onJoinRoom" styleClass="btn-blue"
                    prefWidth="340" maxWidth="340" disable="true">
                    <VBox.margin>
                        <Insets bottom="20" />
                    </VBox.margin>
                </Button>

                <!-- Info Box -->
                <VBox alignment="CENTER" spacing="8" styleClass="blue-section" prefWidth="340">
                    <children>
                        <Label text="💡" style="-fx-font-size: 24;">
                        </Label>
                        <Label text="Ask your friend for the 6-digit room code to join" styleClass="label-secondary"
                            textAlignment="CENTER" wrapText="true">
                        </Label>
                        <Label text="their game!" styleClass="label-secondary" textAlignment="CENTER">
                        </Label>
                    </children>
                </VBox>
            </children>
            <padding>
                <Insets top="30" right="40" bottom="40" left="40" />
            </padding>
        </VBox>
    </children>
</StackPane>